#ifndef TOTCALIBREADER_H
#define TOTCALIBREADER_H

#include "eventbuilder.h"

/*! \brief Read in calibration file paths from file instead of driver.cc. Does this work?
 *
 */
class ToTCalibReader: public EventBuilder {

public:
	ToTCalibReader() {
		name = "ToTCalibReader";
	}
	virtual void init(TbConfig &config);
};

#endif //TOTCALIBREADER_H
